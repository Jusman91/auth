import * as dotenv from 'dotenv';
dotenv.config();

const {
	PORT,
	DATABASE_URL,
	ACCESS_TOKEN_SECRET_KEY,
	FORGOT_PASSWORD_TOKEN_SECRET_KEY,
	CLIENT_URL,
	SENDER_MAIL,
	APP_PASS,
} = process.env;

export {
	PORT,
	DATABASE_URL,
	ACCESS_TOKEN_SECRET_KEY,
	FORGOT_PASSWORD_TOKEN_SECRET_KEY,
	CLIENT_URL,
	SENDER_MAIL,
	APP_PASS,
};
